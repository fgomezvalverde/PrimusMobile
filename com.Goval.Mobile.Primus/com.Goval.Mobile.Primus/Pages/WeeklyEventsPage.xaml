<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="com.Goval.Mobile.Primus.Pages.WeeklyEventsPage">
    <StackLayout>
        <ListView ItemsSource="{Binding WEEKLY_EVENTS}" SelectedItem="{Binding SelectedEvents}" HasUnevenRows="True" SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ffimageloading:CachedImage HeightRequest="180" Aspect="AspectFill"
                                Source="{Binding Media_Path}" ></ffimageloading:CachedImage>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>